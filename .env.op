# Secret references for `op run --env-file=.env.op -- <command>` (1Password CLI). Only references
# live here, never values: op resolves them on this machine, after the owner approves.
SENTRY_AUTH_TOKEN="op://Personal/AI slop arena - Sentry Organisation token/credential"
